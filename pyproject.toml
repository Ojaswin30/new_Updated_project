[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "new_Updated_project"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["ml*"]
